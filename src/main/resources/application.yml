config:
  authentication:
    bearer-token: ${BEARER_TOKEN:null}
  project:
    name: ${PROJECT_NAME:null}
    names: ${PROJECT_NAMES:null}
    use-multi-project: false
  public-ip-site-resolver: ipinfo.io/ip # DO NOT UPDATE
  schedule:
    update-cron: "0 */5 * * * *"
spring:
  application:
    name: DORecordUpdater-ms
  banner:
    image:
      invert: false
      pixelmode: block
      bitdepth: 8

# TEST ENVIRONMENT CONFIGURATION
---
spring:
  config:
    activate:
      on-profile: test
config:
  schedule:
    update-cron: "*/10 * * * * *"